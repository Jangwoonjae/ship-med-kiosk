# 재고현황 DOC 출력 + MSSQL 바코드 실시간 조회 작업지시서

> 프로젝트: `ship-med-kiosk`
> 작업 위치: `C:\projects\ship_med\ship-med-kiosk`

---

## 사전 검토 결과

- **구글 API**: 불필요 (기존 ASP 프로젝트용, 이 프로젝트에서 사용 안 함)
- **MSSQL**: Vercel → 외부 MSSQL 실시간 직접 접속
- **바코드 조회**: 스캔 시마다 MSSQL BarCodeData 테이블 실시간 조회
- **폴백**: 없음 (MSSQL 접속 실패 시 오류 메시지 표시)

---

## ⚠️ Vercel 배포 주의사항 (절대 변경 금지)

- `TURSO_AUTH_TOKEN` — 수정 금지 (변경 시 DB 연결 끊김)
- `Node.js Version` — 반드시 20.x 유지

---

## #1 — MSSQL 바코드 실시간 조회 연동

### 1-1. 패키지 설치

```bash
pnpm add mssql
pnpm add -D @types/mssql
```

### 1-2. MSSQL 연결 모듈 생성

**`lib/mssql.ts`** 신규 생성:

```ts
import sql from 'mssql';

const config: sql.config = {
  server: process.env.MSSQL_HOST!,
  port: parseInt(process.env.MSSQL_PORT ?? '1433'),
  user: process.env.MSSQL_USER!,
  password: process.env.MSSQL_PASS!,
  database: process.env.MSSQL_NAME!,
  options: {
    encrypt: false,
    trustServerCertificate: true,
    connectTimeout: 10000,
    requestTimeout: 10000,
  },
};

let pool: sql.ConnectionPool | null = null;

export async function getMssqlPool(): Promise<sql.ConnectionPool> {
  if (pool && pool.connected) return pool;
  pool = await sql.connect(config);
  return pool;
}

export { sql };
```

### 1-3. 바코드 조회 API Route 수정

**`app/api/barcode/[code]/route.ts`** 를 아래 내용으로 교체:

```ts
import { NextResponse } from 'next/server';
import { getMssqlPool, sql } from '@/lib/mssql';

export async function GET(
  req: Request,
  { params }: { params: { code: string } }
) {
  const barcode = params.code.trim();

  try {
    const pool = await getMssqlPool();

    const result = await pool.request()
      .input('Search', sql.NVarChar(255), barcode)
      .query(`
        SELECT TOP 1
          표준코드문자                                              AS barcode,
          한글상품명 + ' ' +
            CASE WHEN 약품규격 = '없음' THEN ''
                 ELSE ISNULL(약품규격, '') END                     AS productName,
          약품규격                                                  AS spec,
          제품총수량                                                AS packQty,
          제형구분                                                  AS unitName,
          LEFT(RTRIM(ISNULL(업체명,'')), 20)                       AS company,
          ISNULL(비고, '')                                         AS note,
          포장형태                                                  AS packType,
          ISNULL(EDI코드, '')                                      AS ediCode,
          ISNULL(전문일반, '')                                     AS rxOtc
        FROM BarCodeData
        WHERE 표준코드문자 LIKE '%' + @Search + '%'
           OR 대표코드문자 LIKE '%' + @Search + '%'
           OR 구바코드     LIKE '%' + @Search + '%'
      `);

    if (result.recordset.length === 0) {
      return NextResponse.json({
        matched: false,
        message: '등록되지 않은 바코드입니다.',
      });
    }

    const row = result.recordset[0];
    return NextResponse.json({
      matched: true,
      medicine: {
        barcode:   row.barcode,
        name:      row.productName,
        spec:      row.spec,
        packQty:   row.packQty,
        form:      row.unitName,
        company:   row.company,
        note:      row.note,
        packType:  row.packType,
        ediCode:   row.ediCode,
        rxOtc:     row.rxOtc,
      },
    });

  } catch (error) {
    console.error('MSSQL 바코드 조회 오류:', error);
    return NextResponse.json(
      { matched: false, message: 'DB 연결 오류가 발생했습니다.' },
      { status: 500 }
    );
  }
}
```

### 1-4. 환경변수 설정

**`.env.local`** 에 아래 항목 추가 (값은 나중에 직접 입력):

```env
# MSSQL 바코드 DB (나중에 실제 값으로 교체)
MSSQL_HOST=여기에_실제_IP_입력
MSSQL_PORT=1433
MSSQL_USER=여기에_실제_계정_입력
MSSQL_PASS=여기에_실제_비밀번호_입력
MSSQL_NAME=여기에_실제_DB명_입력
```

**`.env.local.example`** 에도 동일 항목 추가 (값은 빈칸):

```env
# MSSQL 바코드 DB
MSSQL_HOST=
MSSQL_PORT=1433
MSSQL_USER=
MSSQL_PASS=
MSSQL_NAME=
```

**Vercel 환경변수 추가** (Settings → Environment Variables):
값은 나중에 직접 브라우저에서 입력할 것

| Name | Value |
|------|-------|
| `MSSQL_HOST` | (나중에 직접 입력) |
| `MSSQL_PORT` | (나중에 직접 입력) |
| `MSSQL_USER` | (나중에 직접 입력) |
| `MSSQL_PASS` | (나중에 직접 입력) |
| `MSSQL_NAME` | (나중에 직접 입력) |

> ⚠️ `.env.local`은 .gitignore에 포함되어 있어 GitHub에 올라가지 않음
> ⚠️ MSSQL 서버에서 Vercel 서버 IP의 1433 포트 접속 허용 필요

---

## #2 — 재고현황 DOC 출력 기능

### 2-1. 패키지 설치

```bash
pnpm add docx
```

### 2-2. DOC 생성 API Route

**`app/api/export/stock-report/route.ts`** 신규 생성:

```ts
import { NextResponse } from 'next/server';
import { db } from '@/lib/db';
import {
  Document, Packer, Paragraph, TextRun,
  Table, TableRow, TableCell,
  AlignmentType, HeadingLevel,
  BorderStyle, WidthType, ShadingType, VerticalAlign,
} from 'docx';

export async function GET(req: Request) {
  const { searchParams } = new URL(req.url);
  const routeType = searchParams.get('route') ?? 'international';
  const isIntl = routeType !== 'domestic';

  const result = await db.execute(`
    SELECT category, name_ko, brand_name, form, strength,
           current_qty,
           ${isIntl ? 'std_intl' : 'std_dom'} as std_qty
    FROM medicines
    ORDER BY category, name_ko
  `);

  const medicines = result.rows as any[];
  const now = new Date();
  const dateStr = `${now.getFullYear()}년 ${now.getMonth()+1}월 ${now.getDate()}일`;

  const border = { style: BorderStyle.SINGLE, size: 1, color: 'AAAAAA' };
  const borders = { top: border, bottom: border, left: border, right: border };
  const margins = { top: 80, bottom: 80, left: 120, right: 120 };

  // A4 기준 컬럼 너비 합계 = 9026 DXA
  const colWidths = [560, 2100, 1800, 800, 1000, 900, 900, 966];
  const headers = ['번호','성분명','상품명','제형','함량','현재고','기준량','상태'];

  const getStatus = (cur: number, std: number) => {
    if (!std) return { text: '-', color: '888888' };
    const r = cur / std;
    if (r <= 0.5) return { text: '긴급', color: 'CC0000' };
    if (r <  0.8) return { text: '경고', color: 'FF6600' };
    return { text: '정상', color: '006600' };
  };

  const makeHeaderRow = () => new TableRow({
    tableHeader: true,
    children: headers.map((text, i) =>
      new TableCell({
        borders, margins,
        width: { size: colWidths[i], type: WidthType.DXA },
        shading: { fill: '1d4ed8', type: ShadingType.CLEAR },
        verticalAlign: VerticalAlign.CENTER,
        children: [new Paragraph({
          alignment: AlignmentType.CENTER,
          children: [new TextRun({ text, bold: true, color: 'FFFFFF', size: 18 })]
        })]
      })
    )
  });

  const makeDataRow = (m: any, idx: number) => {
    const status = getStatus(Number(m.current_qty), Number(m.std_qty));
    const fill = status.text === '긴급' ? 'FFF0F0'
               : status.text === '경고' ? 'FFF8F0' : 'FFFFFF';
    const cells = [
      String(idx + 1),
      m.name_ko ?? '',
      m.brand_name ?? '',
      m.form ?? '',
      m.strength ?? '',
      String(m.current_qty ?? 0),
      String(m.std_qty ?? 0),
      status.text,
    ];
    return new TableRow({
      children: cells.map((text, i) =>
        new TableCell({
          borders, margins,
          width: { size: colWidths[i], type: WidthType.DXA },
          shading: { fill, type: ShadingType.CLEAR },
          children: [new Paragraph({
            alignment: i === 0 || i >= 5 ? AlignmentType.CENTER : AlignmentType.LEFT,
            children: [new TextRun({
              text,
              size: 18,
              color: i === 7 ? status.color : '000000',
              bold: i === 7 && status.text !== '정상',
            })]
          })]
        })
      )
    });
  };

  const categories = ['주사약', '내용약', '외용약'];
  const total = medicines.length;
  const okCnt     = medicines.filter(m => getStatus(Number(m.current_qty), Number(m.std_qty)).text === '정상').length;
  const warnCnt   = medicines.filter(m => getStatus(Number(m.current_qty), Number(m.std_qty)).text === '경고').length;
  const dangerCnt = medicines.filter(m => getStatus(Number(m.current_qty), Number(m.std_qty)).text === '긴급').length;

  const docChildren: any[] = [
    new Paragraph({
      alignment: AlignmentType.CENTER,
      children: [new TextRun({ text: '선내 의약품 재고현황 보고서', bold: true, size: 40 })]
    }),
    new Paragraph({ children: [new TextRun('')] }),
    new Paragraph({
      alignment: AlignmentType.CENTER,
      children: [new TextRun({
        text: `기준일: ${dateStr}  |  항로: ${isIntl ? '국제선' : '국내선'}`,
        size: 22, color: '555555'
      })]
    }),
    new Paragraph({ children: [new TextRun('')] }),
    new Paragraph({
      children: [new TextRun({
        text: `전체 ${total}품목  |  정상 ${okCnt}  |  경고 ${warnCnt}  |  긴급 ${dangerCnt}`,
        size: 22, bold: true
      })]
    }),
    new Paragraph({ children: [new TextRun('')] }),
  ];

  for (const cat of categories) {
    const catMeds = medicines.filter(m => m.category === cat);
    if (!catMeds.length) continue;

    docChildren.push(
      new Paragraph({
        heading: HeadingLevel.HEADING_2,
        children: [new TextRun({ text: `■ ${cat} (${catMeds.length}품목)`, bold: true, size: 26 })]
      }),
      new Table({
        width: { size: 9026, type: WidthType.DXA },
        columnWidths: colWidths,
        rows: [
          makeHeaderRow(),
          ...catMeds.map((m, i) => makeDataRow(m, i))
        ]
      }),
      new Paragraph({ children: [new TextRun('')] }),
    );
  }

  const doc = new Document({
    styles: {
      default: { document: { run: { font: 'Malgun Gothic', size: 20 } } },
      paragraphStyles: [
        {
          id: 'Heading2', name: 'Heading 2', basedOn: 'Normal', next: 'Normal',
          run: { size: 26, bold: true, font: 'Malgun Gothic' },
          paragraph: { spacing: { before: 200, after: 100 }, outlineLevel: 1 }
        }
      ]
    },
    sections: [{
      properties: {
        page: {
          size: { width: 11906, height: 16838 }, // A4
          margin: { top: 1080, right: 1080, bottom: 1080, left: 1080 }
        }
      },
      children: docChildren
    }]
  });

  const buffer = await Packer.toBuffer(doc);

  return new NextResponse(buffer, {
    headers: {
      'Content-Type': 'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
      'Content-Disposition': `attachment; filename*=UTF-8''${encodeURIComponent(`재고현황_${now.toISOString().slice(0,10)}.docx`)}`,
    }
  });
}
```

### 2-3. 관리자 재고현황 탭에 출력 버튼 추가

**`components/admin/StockTable.tsx`** 상단 툴바에 버튼 추가:

```tsx
const handleExport = async () => {
  const routeRes = await fetch('/api/settings');
  const settings = await routeRes.json();
  const route = settings.route_type ?? 'international';

  const res = await fetch(`/api/export/stock-report?route=${route}`);
  if (!res.ok) { alert('문서 생성 실패'); return; }

  const blob = await res.blob();
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `재고현황_${new Date().toISOString().slice(0,10)}.docx`;
  a.click();
  URL.revokeObjectURL(url);
};

// JSX
<button
  onClick={handleExport}
  className="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium"
>
  📄 재고현황 DOC 출력
</button>
```

---

## 최종 작업

```bash
git add .
git commit -m "feat: MSSQL barcode realtime lookup, stock report DOC export"
git push origin master
```

---

## 검증 항목

- [ ] 로컬에서 MSSQL 접속 확인 (`npm run dev` 후 바코드 스캔 테스트)
- [ ] 바코드 스캔 시 한글상품명/규격/업체명 정상 표시
- [ ] 미등록 바코드 스캔 시 "등록되지 않은 바코드" 메시지 표시
- [ ] 관리자 재고현황 탭 "DOC 출력" 버튼 표시
- [ ] DOC 파일 다운로드 후 Word에서 정상 열림
- [ ] 국제선/국내선 기준수량 구분 출력 확인
- [ ] 정상/경고/긴급 색상 구분 확인
- [ ] Vercel 환경변수 입력 후 배포 테스트

---

## 참고: BarCodeData 테이블 구조 (주요 컬럼)

| 컬럼명 | 타입 | 용도 |
|--------|------|------|
| 표준코드문자 | nvarchar(13) | 바코드 조회 키 (GS1-128) |
| 대표코드문자 | nvarchar(13) | 보조 조회 키 |
| 구바코드 | nvarchar(13) | 구형 바코드 |
| 한글상품명 | nvarchar(150) | 상품명 |
| 약품규격 | nvarchar(30) | 규격 |
| 업체명 | nvarchar(50) | 제조/판매사 |
| 제형구분 | nvarchar(10) | 정제/캡슐 등 |
| 포장형태 | nvarchar(10) | 포장 형태 |
| 제품총수량 | nvarchar(10) | 총 포장 수량 |
| EDI코드 | nvarchar(10) | EDI 코드 |
| 전문일반 | nvarchar(10) | 전문의약품/일반의약품 구분 |
